# Secrets Template File
# Copy this file to .env.secrets and set secure values
# This file should NEVER be committed to version control

# SECURITY WARNING: Change all these values before deploying to production!

# JWT Secret - Must be at least 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_A_SECURE_32_CHAR_SECRET_KEY_FOR_PRODUCTION

# Database Passwords
# Generate with: openssl rand -base64 16
MYSQL_ROOT_PASSWORD=CHANGE_ME_TO_SECURE_ROOT_PASSWORD
MYSQL_PASSWORD=CHANGE_ME_TO_SECURE_APP_PASSWORD

# RabbitMQ Credentials
# Generate with: openssl rand -base64 12
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=CHANGE_ME_TO_SECURE_RABBITMQ_PASSWORD

# Redis Password (optional, for secured Redis)
# Generate with: openssl rand -base64 16
REDIS_PASSWORD=CHANGE_ME_TO_SECURE_REDIS_PASSWORD

# Additional Security Notes:
# 1. Use strong, unique passwords for each service
# 2. Consider using a secrets management system in production
# 3. Rotate secrets regularly
# 4. Never share secrets in plain text
# 5. Use environment-specific secrets for dev/staging/prod